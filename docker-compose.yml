services:
  app:
    build: .
    ports:
      - "6348:6348"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=6348
      - HOST=0.0.0.0
    volumes:
      - ./services.json:/app/services.json:ro
    restart: unless-stopped
